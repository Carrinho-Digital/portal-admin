export default (values) => {
    let errors = {};

    // if (!values.email || !values.email.trim()) {
    //     errors.nome = 'Digite seu e-mail!';
    // }

    // if (!values.password || !values.password.trim()) {
    //     errors.sobrenome = 'Digite sua senha!';
    // }
    
    return errors;
}